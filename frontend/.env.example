# Frontend Environment Variables
# For production, specify the backend API URL.
# If hosting frontend and backend on the same domain with a reverse proxy, you can leave this empty (it defaults to /api)
# VITE_API_URL=https://api.yourdomain.com/api
